const TIMELINE_SCALE = 30; // 1 second = 30px - set it to minimum 30px for good visibility
const DURATION = 5 * 60; // in seconds

export const config = {
  timelineScale: TIMELINE_SCALE,
  duration: DURATION,
  width: TIMELINE_SCALE * DURATION
};
